# DINO 三模态改造 — 详细实施计划

> 编制时间：2026-07-14
> 参照文档：`微调计划.txt`

---

## 工程总览

```
阶段〇 (数据流) ──→ 阶段一 (编码器) ──→ 阶段二 (融合) ──→ 阶段三 (损失)
                                                    │
                                                    ↓
                                              阶段四 (配置/训练)
                                                    │
                                                    ↓
                                         阶段五 (增强) ← 可并行
                                                    │
                                                    ↓
                                         阶段六 (提交格式)
```

**阻塞关系**：阶段二是核心瓶颈——融合模块不通，后续都无法验证。
阶段四依赖阶段三（损失函数定义好才能配权重），阶段五可与阶段四并行。

---

## 阶段〇：基础修复（数据流打通）

### `dataset/dataset.py`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 0a | **删除调试代码** | L104 `print(stem)`, L154-156 末尾测试代码 | 生产环境不应有 print |
| 0b | **修复 `target['size']`** | L132 | 当前写死 `orig_size`，应改为 `target_size`（即 resize 后的实际尺寸） |
| 0c | **增加 `num_classes` 参数** | `__init__` | 构造函数加 `num_classes=12`，便于后续标签验证 |
| 0d | **标签验证** | `__getitem__` 末尾 | 检查 boxes 坐标在 [0,1] 范围，类别在 [0, num_classes-1] |
| 0e | **自动生成光照/密度标签** | `__getitem__` build target 时 | 光照：RGB 均值阈值（bright/dim/dark）；密度：GT 框数阈值（sparse/medium/dense） |

### `util/misc.py`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 0f | **新增 `collate_fn_multimodal`** | 文件末尾 | 三模态专用 collate：对 samples 的每个 key（rgb/ir/depth）各自 stack；targets 保持原逻辑 |

```python
def collate_fn_multimodal(batch):
    # batch = list of dicts
    # each dict = {'rgb': [3,H,W], 'ir': [3,H,W], 'depth': [1,H,W], 'target': {...}}
    samples = {}
    for key in ['rgb', 'ir', 'depth']:
        samples[key] = torch.stack([item[key] for item in batch])
    targets = [item['target'] for item in batch]
    return samples, targets
```

### `main.py`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 0g | **根据 `args.dataset_file` 选择 collate_fn** | L180-183 DataLoader 构建处 | `dataset_file == 'multimodal'` 时用 `collate_fn_multimodal` |

### `datasets/__init__.py`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 0h | **新增 `multimodal` 导入分支** | `build_dataset()` 函数 | `if args.dataset_file == 'multimodal': from .multimodal import build_multimodal; return build_multimodal(image_set, args)` |

### `datasets/multimodal.py`（**新建文件**）

| # | 改动 | 说明 |
|---|------|------|
| 0i | **创建文件** | 实现 `build_multimodal(image_set, args)`，调用 `dataset/dataset.py` 的 `Multimodeldataset`，支持 train/val 按比例划分，返回 train 或 val 子集 |

---

## 阶段一：模态编码器

### `models/multimodal_encoders.py`（**新建文件**）

| # | 组件 | 说明 |
|---|------|------|
| 1a | **`LightweightEncoder` 类** | 轻量编码器：3 层 Conv+BN+ReLU。结构：in_channels → 64 → 128 → 256，每层 3×3 卷积，后两层 stride=2 下采样 |

```python
class LightweightEncoder(nn.Module):
    """轻量编码器：3层Conv+BN+ReLU，输出256维，空间尺寸变为1/4"""
    def __init__(self, in_channels):
        super().__init__()
        self.conv = nn.Sequential(
            nn.Conv2d(in_channels, 64, 3, padding=1),
            nn.BatchNorm2d(64),
            nn.ReLU(inplace=True),
            nn.Conv2d(64, 128, 3, padding=1, stride=2),   # H/2
            nn.BatchNorm2d(128),
            nn.ReLU(inplace=True),
            nn.Conv2d(128, 256, 3, padding=1, stride=2),   # H/4
            nn.BatchNorm2d(256),
            nn.ReLU(inplace=True),
        )

    def forward(self, x):
        return self.conv(x)
```

### `models/dino/backbone.py`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 1b | **实现 `build_ir_encoder`** | L146-147 | 当前只写 `return`。改为：`from models.multimodal_encoders import LightweightEncoder; return LightweightEncoder(in_channels=3)` |
| 1c | **实现 `build_depth_encoder`** | L148-149 | 当前只写 `return`。改为：`return LightweightEncoder(in_channels=1)` |

---

## 阶段二：Scene-Aware Fusion（核心模块）

### `models/scene_fusion.py`（**新建文件**）

| # | 子模块 | 输入 | 输出 | 实现 |
|---|--------|------|------|------|
| 2a | **Global Scene Query** | RGB 多尺度特征均值 [B,256] | scene_repr [B,256] | 1 个可学习 query [1,256] + MultiheadAttention 聚合全局上下文 |
| 2b | **Lighting Head** | scene_repr [B,256] | 3 类 logits | `nn.Linear(256, 3)` |
| 2c | **Density Head** | scene_repr [B,256] | 3 类 logits | `nn.Linear(256, 3)` |
| 2d | **Weight Generator** | scene_repr [B,256] | [w_rgb, w_ir, w_depth] | MLP(256→128→3) → softmax。gate 偏置初始化为 -2.0，使初始融合接近纯 RGB |
| 2e | **Spatial Modulator** | IR+Depth concat [B,512,H,W] | spatial_attn [B,1,H,W] | Conv(512→128→1) + Sigmoid |
| 2f | **Multi-Scale Fusion** | RGB[N]+IR+Depth+weights+spatial | fused [N, B,256,H_i,W_i] | 对每个尺度 interpolate IR/Depth → 加权 sum → 乘 spatial_attn |

完整类结构参见下文"核心代码块"。

### `models/dino/dino.py` — 修改 `__init__()`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 2g | **保存编码器引用** | L120-121 之后 | 添加 `self.ir_encoder = ir_encoder`、`self.depth_encoder = depth_encoder` |
| 2h | **判断是否三模态** | 同上 | `self.is_multimodal = (ir_encoder is not None) and (depth_encoder is not None)` |
| 2i | **创建 SceneFusion** | 同上 | `if self.is_multimodal: self.scene_fusion = SceneQueryFusion(hidden_dim)`，否则 `self.scene_fusion = None` |

### `models/dino/dino.py` — 修改 `forward()`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 2j | **IR/Depth 编码** | L236-246 区域（dict 输入分支） | 在当前注释 `# 先预留，后续融合用` 处，调用 `self.ir_encoder(ir)` 和 `self.depth_encoder(depth)` |
| 2k | **SceneFusion 调用** | L249 `features, poss = self.backbone(samples)` 之后 | 提取 RGB 各层 tensor → `fused, scene_outputs = self.scene_fusion(rgb_features, ir_feat, depth_feat)` → 替换 features 中的 tensor |
| 2l | **scene_outputs 传递** | forward 末尾 return | 将 `scene_outputs`（含 light_logits/density_logits/modality_weights）存入 `out` dict 中返回给 criterion |

**L236-270 区域修改后的完整逻辑：**

```python
if isinstance(samples, dict):
    rgb = samples['rgb']          # [B, 3, H, W]
    ir = samples['ir']            # [B, 3, H, W]
    depth = samples['depth']      # [B, 1, H, W]

    # 编码 IR 和 Depth
    ir_feat = self.ir_encoder(ir)        # [B, 256, H/4, W/4]
    depth_feat = self.depth_encoder(depth)  # [B, 256, H/4, W/4]

    # 构造 NestedTensor 给 Backbone
    B, C, H, W = rgb.shape
    mask = torch.zeros((B, H, W), dtype=torch.bool, device=rgb.device)
    samples = NestedTensor(rgb, mask)

elif isinstance(samples, (list, torch.Tensor)):
    samples = nested_tensor_from_tensor_list(samples)
    ir_feat = None
    depth_feat = None

features, poss = self.backbone(samples)

# === 三模态融合 ===
if ir_feat is not None and depth_feat is not None and self.scene_fusion is not None:
    rgb_features = [f.tensors for f in features]  # list of [B,256,H_i,W_i]
    fused_features, scene_outputs = self.scene_fusion(rgb_features, ir_feat, depth_feat)
    for i, (feat, fused) in enumerate(zip(features, fused_features)):
        features[i] = NestedTensor(fused, feat.mask)
    self._last_scene_outputs = scene_outputs
else:
    self._last_scene_outputs = None

# ... 后续 srcs/masks/input_proj 保持不变 ...
```

### `models/dino/dino.py` — 修改 `build_dino()`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 2m | **`dn_labelbook_size` 自动适配** | L734/L771 附近 | 确保 `dn_labelbook_size >= num_classes + 1`（微调计划要求） |
| 2n | **按阶段控制冻结** | 函数末尾 return 之前 | 根据 `args.freeze_backbone`、`args.freeze_enc_layers` 设置 `requires_grad` |

---

## 阶段三：辅助损失

### `models/dino/dino.py` — 修改 `SetCriterion.forward()`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 3a | **提取 scene_outputs** | `forward()` 开头 | `scene_outputs = outputs.get('scene_outputs', None)` |
| 3b | **计算 lighting 损失** | loss 累加处 | `loss_scene_lighting = F.cross_entropy(scene_outputs['light_logits'], light_labels)` × 权重 0.1 |
| 3c | **计算 density 损失** | loss 累加处 | `loss_scene_density = F.cross_entropy(scene_outputs['density_logits'], density_labels)` × 权重 0.1 |
| 3d | **计算权重熵正则** | loss 累加处 | `loss_weight_entropy = -Σ w_i · log(w_i)` × 权重 0.01 |
| 3e | **labels 来源** | — | 光照/密度标签在 `dataset/dataset.py` 阶段〇中已自动生成，存储在 target dict 里 |

---

## 阶段四：配置与训练

### `config/DINO/DINO_4scale_multimodal.py`（**新建文件**）

| # | 配置项 | 值 | 说明 |
|---|--------|-----|------|
| 4a | `_base_` | `['DINO_4scale.py']` | 继承 4scale 基准配置 |
| 4b | `dataset_file` | `'multimodal'` | 触发三模态数据加载 |
| 4c | `num_classes` | `13` | 12 类 + ∅ |
| 4d | `dn_labelbook_size` | `13` | DN 标签表大小 ≥ num_classes |
| 4e | `lr` | `1e-4` | 新模块学习率 |
| 4f | `lr_backbone` | `0` | 冻结 RGB Backbone |
| 4g | `epochs` | `50` | 总训练轮数 |
| 4h | `lr_drop_list` | `[20, 40]` | 阶段二 L20→0.1x，阶段三 L40→0.1x |
| 4i | `scene_loss_coef` | `0.1` | 辅助损失权重 |
| 4j | `weight_entropy_coef` | `0.01` | 权重熵正则系数 |
| 4k | `batch_size` | `2` | V100 16GB 极限 |
| 4l | `param_dict_type` | `'multimodal'` | 自定义分层学习率策略 |
| 4m | `freeze_backbone` | `True` | 阶段一/二冻结 |
| 4n | `freeze_enc_layers` | `4` | Encoder 前 4 层冻结 |
| 4o | `coco_path` | 实际数据路径 | 训练数据根目录 |

### `util/get_param_dicts.py`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 4p | **新增 `param_dict_type = 'multimodal'`** | `get_param_dict()` 函数 | 分层：ir_encoder/depth_encoder/scene_fusion 用 `lr`；transformer 后 2 层 Enc + 全部 Decoder 用 `lr * 0.1`；所有 backbone 参数 freeze |

```python
if param_dict_type == 'multimodal':
    param_dicts = [
        # 新模块：正常学习率
        {"params": [p for n, p in model_without_ddp.named_parameters()
                    if ('ir_encoder' in n or 'depth_encoder' in n or 'scene_fusion' in n)
                    and p.requires_grad],
         "lr": args.lr},
        # Decoder + Encoder后2层：低学习率微调
        {"params": [p for n, p in model_without_ddp.named_parameters()
                    if ('transformer' in n or 'bbox_embed' in n or 'class_embed' in n
                        or 'input_proj' in n or 'label_enc' in n or 'tgt_embed' in n)
                    and p.requires_grad],
         "lr": args.lr * 0.1},
    ]
    return param_dicts
```

---

## 阶段五：数据增强

### `dataset/dataset.py`

| # | 改动 | 位置 | 说明 |
|---|------|------|------|
| 5a | **同步 RandomHorizontalFlip** | `__getitem__` 中 resize 之后 | p=0.5，三模态同步翻转；boxes 的 cx 需翻转（1 - cx） |
| 5b | **同步多尺度 Resize** | `__getitem__` 中 | 训练时从 [640, 800, 1024, 1280] 随机选一个 scale，三模态同步 resize |
| 5c | **模态 Dropout** | `__getitem__` 末尾 | p=0.2 随机将 depth 或 ir 全图置 0（模拟单模态缺失的鲁棒性） |
| 5d | **IR 温度扰动** | `__getitem__` 中 IR 加载后 | `ir = ir * random(0.8, 1.2) + random(-20, 20) / 255` |
| 5e | **Depth 噪声** | `__getitem__` 中 depth 归一化后 | `depth = depth + N(0, 0.01)`，只加在有效深度区域 |

### `util/misc.py`

| # | 改动 | 说明 |
|---|------|------|
| - | 无需额外改动 | collate_fn 已在阶段〇处理 |

---

## 阶段六：提交格式

### `tools/submit.py`（**新建文件**）

| # | 功能 | 说明 |
|---|------|------|
| 6a | **批量推理** | 遍历测试集图片 → 模型推理 → 后处理 |
| 6b | **输出格式** | 每张图一个同名 txt：`[class_id, cx, cy, w, h, confidence]`，最多 100 个框 |
| 6c | **空文件处理** | 无目标时提交空 txt 文件 |

---

## 涉及文件清单

### 新建文件（5 个）

| 文件 | 阶段 | 核心内容 |
|------|------|----------|
| `datasets/multimodal.py` | 〇 | 三模态 dataset 构建函数 |
| `models/multimodal_encoders.py` | 一 | LightweightEncoder（IR 和 Depth 共用） |
| `models/scene_fusion.py` | 二 | SceneQueryFusion（核心创新模块） |
| `config/DINO/DINO_4scale_multimodal.py` | 四 | 三模态训练配置 |
| `tools/submit.py` | 六 | 测试集推理 + 比赛提交格式 |

### 修改文件（7 个）

| 文件 | 阶段 | 改动量 | 改动内容 |
|------|------|--------|----------|
| `dataset/dataset.py` | 〇 + 五 | 中 | 清理调试代码、修复 size、自动标签、数据增强 |
| `util/misc.py` | 〇 | 小 | 新增 collate_fn_multimodal |
| `main.py` | 〇 | 小 | 选择 collate_fn |
| `datasets/__init__.py` | 〇 | 小 | 新增 multimodal 分支 |
| `models/dino/backbone.py` | 一 | 小 | 实现 build_ir_encoder / build_depth_encoder |
| `models/dino/dino.py` | 二 + 三 + 四 | 大 | forward 融合逻辑、辅助损失、冻结策略、build_dino |
| `util/get_param_dicts.py` | 四 | 小 | 新增 multimodal 分层学习率 |

---

## 核心代码块（准备就绪）

### SceneQueryFusion 完整结构

```python
import torch
import torch.nn as nn
import torch.nn.functional as F


class SceneQueryFusion(nn.Module):
    """
    Scene-Aware Fusion Module
    输入: RGB多尺度特征[list of [B,256,Hi,Wi]], IR特征[B,256,H,W], Depth特征[B,256,H,W]
    输出: 融合后的多尺度特征 + 辅助预测
    """
    def __init__(self, hidden_dim=256, nheads=8):
        super().__init__()
        # 1. Learnable scene query
        self.scene_query = nn.Parameter(torch.randn(1, 1, hidden_dim))

        # 2. Cross-attention for global scene context
        self.cross_attn = nn.MultiheadAttention(hidden_dim, nheads, batch_first=True)

        # 3. Scene classification heads
        self.lighting_head = nn.Linear(hidden_dim, 3)   # bright / dim / dark
        self.density_head = nn.Linear(hidden_dim, 3)    # sparse / medium / dense

        # 4. Modality weight generator (bias initialized toward RGB)
        self.weight_mlp = nn.Sequential(
            nn.Linear(hidden_dim, 128),
            nn.ReLU(inplace=True),
            nn.Linear(128, 3),  # w_rgb, w_ir, w_depth
        )
        # 初始化偏置为负 → 初始 softmax 接近 [1, 0, 0]（纯 RGB）
        nn.init.constant_(self.weight_mlp[-1].bias.data, -2.0)

        # 5. Spatial modulator: depth边缘 + IR局部对比度 → 空间注意力
        self.spatial_conv = nn.Sequential(
            nn.Conv2d(512, 128, 3, padding=1),
            nn.ReLU(inplace=True),
            nn.Conv2d(128, 1, 1),
            nn.Sigmoid(),
        )

    def forward(self, rgb_features, ir_feat, depth_feat):
        """
        Args:
            rgb_features: list of [B, 256, Hi, Wi]   (多尺度 RGB)
            ir_feat:      [B, 256, H, W]
            depth_feat:   [B, 256, H, W]
        Returns:
            fused_features: list of [B, 256, Hi, Wi]  (多尺度融合特征)
            outputs: dict with scene predictions
        """
        B = rgb_features[0].shape[0]

        # --- 1. Global Scene Query ---
        # 对各尺度RGB做全局平均 → 聚合为 [B, 256]
        rgb_global = []
        for f in rgb_features:
            rgb_global.append(f.mean(dim=[2, 3]))  # [B, 256]
        rgb_global = torch.stack(rgb_global, dim=1)  # [B, N_scales, 256]

        scene_q = self.scene_query.expand(B, -1, -1)  # [B, 1, 256]
        scene_repr, _ = self.cross_attn(scene_q, rgb_global, rgb_global)
        scene_repr = scene_repr.squeeze(1)  # [B, 256]

        # --- 2. Scene predictions ---
        light_logits = self.lighting_head(scene_repr)     # [B, 3]
        density_logits = self.density_head(scene_repr)    # [B, 3]

        # --- 3. Modality weights ---
        modality_weights = self.weight_mlp(scene_repr).softmax(dim=-1)  # [B, 3]
        w_rgb   = modality_weights[:, 0].view(B, 1, 1, 1)
        w_ir    = modality_weights[:, 1].view(B, 1, 1, 1)
        w_depth = modality_weights[:, 2].view(B, 1, 1, 1)

        # --- 4. Spatial attention ---
        ir_depth = torch.cat([ir_feat, depth_feat], dim=1)  # [B, 512, H, W]
        spatial_attn = self.spatial_conv(ir_depth)           # [B, 1, H, W]

        # --- 5. Multi-scale fusion ---
        fused_features = []
        for rgb_f in rgb_features:
            Hi, Wi = rgb_f.shape[-2:]

            # 对齐尺寸
            ir_i = F.interpolate(ir_feat, size=(Hi, Wi), mode='bilinear', align_corners=False)
            d_i  = F.interpolate(depth_feat, size=(Hi, Wi), mode='bilinear', align_corners=False)
            spat_i = F.interpolate(spatial_attn, size=(Hi, Wi), mode='bilinear', align_corners=False)

            # 加权融合
            fused = w_rgb * rgb_f + w_ir * ir_i + w_depth * d_i
            fused = fused * spat_i  # 空间调制

            fused_features.append(fused)

        scene_outputs = {
            'light_logits': light_logits,
            'density_logits': density_logits,
            'modality_weights': modality_weights,
        }

        return fused_features, scene_outputs
```

---

## 分轮次实施建议

| 轮次 | 阶段 | 涉及文件 | 新增 | 修改 | 预计工作量 |
|------|------|----------|------|------|-----------|
| **第1轮** | 〇 + 一 | 8 个 | 2 | 6 | 打通数据流，编解码器就位 |
| **第2轮** | 二 | 3 个 | 2 | 1 | 核心融合，前向能跑通 |
| **第3轮** | 三 + 四 | 5 个 | 2 | 3 | 损失+配置，训练能跑通 |
| **第4轮** | 五 + 六 | 2 个 | 1 | 1 | 数据增强 + 提交脚本 |
